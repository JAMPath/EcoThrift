# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EcoThrift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EcoThrift
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'FirebaseUI'
  pod 'AlamofireImage'

  target 'EcoThriftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EcoThriftUITests' do
    # Pods for testing
  end

end
